# Latte LP Design System

> VoiceLatte LP + Latteシリーズハブページのデザインシステム。
> Starbucksの「カフェ素材の温かみ」× Ollamaの「ドキュメント的ミニマリズム」を掛け合わせた設計。
> コーヒーブラウンの色温度で包みつつ、装飾を排して余白で語る。

---

## 1. 設計思想

**温かみのあるミニマリズム。** カフェの空間設計のように、素材と余白で品質を伝える。

- **Ollamaから継承:** 単一カラムの読み物的レイアウト、大胆なセクション間余白（80px+）、装飾の徹底排除、コンテンツが構造そのもの
- **Starbucksから継承:** 温かみのあるクリーム系キャンバス（純白を使わない）、カラーブロックでのセクションリズム、pill型ボタン、多層低alphaシャドウ
- **VoiceLatte独自:** コーヒーブラウンの5段階パレット、セリフ体のディスプレイフォント、OSSプロダクトとしての誠実さ

**ページ全体のリズム:**
クリームヒーロー → 白系コンテンツ → ウォームバンド（window-bg） → 白系コンテンツ → エスプレッソフッター

グラデーションはヒーローセクションのみ許容。他のセクションはソリッドカラーブロック。

---

## 2. カラーパレット

### ブランドカラー（コーヒー5段階）

| トークン | 値 | 役割 |
|---------|------|------|
| `--espresso` | `#5C3928` | 最も濃いブラウン。見出し・本文テキスト・Primary CTAの背景・フッター背景 |
| `--dark-coffee` | `#6F4C3E` | 二次テキスト。本文の補助色、タグライン、説明文 |
| `--latte` | `#C1A07B` | アクセント。ボーダー、セカンダリボタンのボーダー、フッターリンク |
| `--cream` | `#E8D5A1` | 装飾的サーフェス。ヒーローグラデーションの終点、プライバシーポリシーのhr |
| `--milk` | `#F2E0C0` | ヒーローグラデーションの中間色 |

### サーフェス

| トークン | 値 | 役割 |
|---------|------|------|
| `--window-bg` | `#FDF2D2` | ウォームバンド背景。Starbucksの`#f2f0eb`に相当する「カフェの壁」色 |
| `--field-bg` | `#FBFAF8` | ページキャンバス。純白ではなくわずかにウォームなオフホワイト |
| `--white` | `#FFFFFF` | カード・モーダルのサーフェス |

### セマンティック

| トークン | 値 | 役割 |
|---------|------|------|
| `--success` | `#599A4D` | プライバシーバッジのボーダー（「100% オンデバイス」の信頼シグナル） |

### テキストカラー運用

- **見出し:** `--espresso` を直接使用（純黒は使わない — Starbucksの`rgba(0,0,0,0.87)`と同じ発想で、ウォームキャンバスに合う色温度を維持）
- **本文・説明:** `--dark-coffee`
- **ダークサーフェス上:** `--cream`（プライマリ）、`--latte`（リンク）
- **最低コントラスト:** `--latte` on `--white` は装飾用途のみ。読ませるテキストには使わない

### 使わないもの

- 純黒 `#000000` — テキストにもCTAにも使わない。`--espresso` が最暗色
- 純白キャンバス — ページ背景は常に `--field-bg`
- ブルー・パープル等の寒色 — パレット外
- グラデーション（ヒーロー以外）

---

## 3. タイポグラフィ

### フォントファミリー

| 用途 | ファミリー | 補足 |
|------|-----------|------|
| ディスプレイ | `'DM Serif Display', serif` | ヒーロー見出し・セクション見出しのみ。Starbucksの「Lander Tall」的な温かみのあるセリフ |
| 本文 | `'Noto Sans JP', 'Hiragino Sans', sans-serif` | 日本語の可読性最優先。すべての本文・ボタン・キャプション |

**フォント切り替えルール:** DM Serif Displayはh1とセクションh2のみ。h3以下・本文・ボタンラベルには絶対に使わない。Ollamaが「SF Pro Roundedは見出しだけ、あとはsystem-ui」と徹底しているのと同じ規律。

### 階層

| 役割 | サイズ | ウェイト | 行高 | 字間 | ファミリー |
|------|--------|---------|------|------|-----------|
| ヒーロー見出し | `clamp(2.5rem, 6vw, 4rem)` | 400 | 1.2 | `0.02em` | DM Serif Display |
| セクション見出し | `2rem` | 400 | 1.3 | `0.02em` | DM Serif Display |
| カード見出し | `1.1rem`〜`1.25rem` | 700 | 1.4 | 0 | Noto Sans JP |
| タグライン | `clamp(1rem, 2.5vw, 1.25rem)` | 400 | 1.6 | 0 | Noto Sans JP |
| 本文 | `0.9rem`〜`0.95rem` | 400 | 1.7〜1.8 | 0 | Noto Sans JP |
| ボタンラベル | `0.95rem` | 500 | 1.2 | 0 | Noto Sans JP |
| キャプション | `0.8rem`〜`0.85rem` | 400 | 1.5 | 0 | Noto Sans JP |

### 日本語タイポグラフィ原則

- **行間:** 本文は `line-height: 1.7`〜`1.8`。日本語は1.5では詰まりすぎる
- **字間:** 本文は `letter-spacing: 0`（Noto Sans JPのデフォルトメトリクスを信頼）。見出しセリフのみ `0.02em`
- **禁則処理:** `word-break: auto-phrase`（対応ブラウザ）または `overflow-wrap: break-word`
- **フォントスムージング:** `-webkit-font-smoothing: antialiased` 全体適用

---

## 4. スペーシング

### スケール

| トークン名 | 値 | 用途 |
|-----------|------|------|
| `--space-xs` | `4px` | アイコンとラベルの間 |
| `--space-sm` | `8px` | カード内要素間の最小ギャップ |
| `--space-md` | `16px` | カード内パディング、ボタン内パディング |
| `--space-lg` | `24px` | カード間ギャップ、セクション内コンテンツ間 |
| `--space-xl` | `32px` | カードパディング |
| `--space-2xl` | `48px` | セクション内部パディング（モバイル） |
| `--space-section` | `80px`〜`96px` | **セクション間の余白。Ollamaの88pxに相当。LPの呼吸を決める最重要トークン** |

### 余白の哲学

Ollamaと同様、**セクション間は余白のみで区切る**。装飾的なディバイダー線は使わない（プライバシーポリシーページ内の`<hr>`は例外）。`--space-section` の空白がセクションの境界そのもの。

### コンテナ幅

| ページ | 最大幅 | 補足 |
|--------|--------|------|
| トップ（Latteハブ） | `640px` | 単一カラム、アプリカード中心 |
| VoiceLatte LP | `1000px`（フィーチャーグリッド）/ `800px`（Pro・テキストセクション） | グリッドは広め、テキストは狭め |
| プライバシーポリシー | `720px` | 読み物として最適な幅 |

---

## 5. コンポーネント

### ボタン

**Primary（エスプレッソpill）**
- 背景: `--espresso`
- テキスト: `--white`
- radius: `100px`（full pill — Starbucksの50px pill / Ollamaの9999pxと同じ発想）
- padding: `0.875rem 2rem`
- font: Noto Sans JP, 0.95rem, weight 500
- transition: `all 0.2s cubic-bezier(0.25, 1, 0.5, 1)`
- hover: 背景 `--dark-coffee` + `translateY(-2px)` + shadow `0 8px 24px rgba(92,57,40,0.25)`
- active: `transform: scale(0.97)` — Starbucksの`scale(0.95)`をやや控えめに

**Secondary（アウトラインpill）**
- 背景: `--white`
- テキスト: `--espresso`
- border: `1.5px solid var(--latte)`
- radius / padding / font: Primaryと同じ
- hover: 背景 `--window-bg` + `translateY(-2px)`

**ダークサーフェス上のボタン（フッター内リンク等）**
- テキスト: `--latte`
- hover: `text-decoration: underline`

### カード

**フィーチャーカード**
- 背景: `--white`
- border: `1px solid rgba(193, 160, 123, 0.2)` — Starbucksの「ささやくようなシャドウ」をボーダーで表現
- radius: `16px` — Starbucksの12pxよりやや大きく、温かみを強調
- padding: `2rem`
- hover: `translateY(-4px)` + shadow `0 12px 32px rgba(92, 57, 40, 0.08)`
- 構造: アイコン（絵文字 2rem）→ h3 → p

**アプリカード（トップページ）**
- フィーチャーカードと同じサーフェス仕様
- レイアウト: 横並び（アイコン画像 80px + テキスト情報）
- アイコン: `border-radius: 18%` + shadow `0 4px 16px rgba(92,57,40,0.12)`
- モバイル: 縦積み + テキスト中央揃え

**Proフィーチャーカード**
- 背景: `--window-bg`（白カードとの対比でウォームに）
- border: なし
- radius: `16px`
- padding: `2rem`

### バッジ

**プライバシーバッジ（「100% オンデバイス処理」）**
- 背景: `--white`
- border: `1.5px solid var(--success)`
- radius: `100px`（pill）
- padding: `1rem 2rem`
- font-weight: 500
- アイコン: 🟢 絵文字 1.5rem

### セクションバンド

**ウォームバンド**
- 背景: `--window-bg`
- 全幅
- padding: `4rem 2rem`
- テキスト中央揃え
- 用途: プライバシーセクション等、強調したい単一メッセージ

**エスプレッソバンド（フッター）**
- 背景: `--espresso`
- テキスト: `--cream`（プライマリ）/ `--latte`（リンク）
- StarbucksのHouse Green (`#1E3932`)帯と同じ役割 — 暗いブックエンドでページを締める

### ヒーロー

- 背景: `linear-gradient(160deg, var(--window-bg) 0%, var(--milk) 50%, var(--cream) 100%)` — **LPで唯一のグラデーション**
- 装飾: `::before` で `radial-gradient` のぼかし円を右上に配置（控えめ、0.3 alpha）
- アイコン画像: `border-radius: 22%` + shadow + float animation（3s ease-in-out）
- レイアウト: 完全中央揃え、max-width 800px
- 高さ: `min-height: 100vh`（VoiceLatteページ）/ 自動（トップページ）

### ナビゲーション

現時点ではグローバルナビなし。プライバシーページのみヘッダーにロゴリンクあり。
Latteシリーズが増えた場合に備え、以下を想定：
- 高さ: `56px`〜`64px`
- 背景: `--field-bg`（Ollamaのcanvas白に相当）
- border-bottom: `1px solid rgba(193,160,123,0.2)`
- リンク: `--espresso` テキスト、weight 500

### フッター

- 背景: `--espresso`
- テキスト: `--cream`
- リンク: `--latte`、hover で underline
- padding: `2rem`〜`3rem`
- リンク群: flex row, gap 2rem, center, wrap
- コピーライト: 0.8rem, opacity 0.7

---

## 6. エレベーション（深度）

Ollamaの「シャドウゼロ」とStarbucksの「多層低alphaシャドウ」の中間を取る。

| レベル | 処理 | 用途 |
|--------|------|------|
| 0 — フラット | border `1px solid rgba(193,160,123,0.2)` のみ | カードのデフォルト状態 |
| 1 — ホバーリフト | shadow `0 12px 32px rgba(92,57,40,0.08)` + `translateY(-4px)` | カードのhover |
| 2 — アイコン | shadow `0 4px 16px rgba(92,57,40,0.12)` 常時 | アプリアイコン、ヒーローアイコン |
| 3 — CTA hover | shadow `0 8px 24px rgba(92,57,40,0.25)` | Primary ボタンのhover |

**シャドウの色は常に `rgba(92,57,40, _)` — エスプレッソ色ベース。** 純黒のシャドウは使わない。

---

## 7. 角丸スケール

| 値 | 用途 |
|----|------|
| `100px` (pill) | ボタン、バッジ、インラインタグ |
| `22%` | アプリアイコン画像（macOSアイコンの角丸に近似） |
| `18%` | 小さめのアプリアイコン |
| `16px` | カード、Proフィーチャーカード |
| `12px` | プライバシーページのcontact枠 |

Ollamaの「pillかカードか、2択」の規律を踏襲。中間的な角丸は作らない。

---

## 8. アニメーション

| 名前 | 値 | 用途 |
|------|------|------|
| ボタン transition | `all 0.2s cubic-bezier(0.25, 1, 0.5, 1)` | 全ボタン共通 |
| カード hover | `transform 0.2s, box-shadow 0.2s` | カード浮き上がり |
| ヒーローfloat | `3s ease-in-out infinite` translateY 0→-10px→0 | アイコンの浮遊 |
| active press | `transform: scale(0.97)` | ボタン押下フィードバック |

**アニメーションの原則:**
- ease-in-outベース。springやbounceは使わない
- 200ms以下の高速遷移。ユーザーを待たせない
- ヒーローのfloatは唯一の装飾的アニメーション。他の「動く装飾」は追加しない

---

## 9. レスポンシブ

### ブレークポイント

| 名前 | 幅 | 変更点 |
|------|------|--------|
| desktop | 1000px+ | フィーチャーグリッド3列、Pro 2列横並び |
| tablet | 600px〜999px | フィーチャーグリッド2列、余白やや縮小 |
| mobile | 〜599px | フィーチャーグリッド1列、アプリカード縦積み、ヒーローpadding縮小 |

### 折りたたみ戦略

- **フィーチャーグリッド:** `repeat(auto-fit, minmax(280px, 1fr))` でCSS Grid自動折り返し
- **Pro機能カード:** flex wrap で自動折り返し
- **アプリカード（トップ）:** 600px以下で `flex-direction: column` + `text-align: center`
- **セクション余白:** desktop `--space-section` → mobile は `4rem`程度に縮小
- **ヒーロー見出し:** `clamp()` でfluid typography。明示的なブレークポイント切り替えなし

---

## 10. Do's and Don'ts

### Do

- ページキャンバスに `--field-bg` を使う（純白ではなくウォームオフホワイト）
- ボタンは常に `100px` pill radius
- シャドウ色はエスプレッソベース `rgba(92,57,40,_)`
- セクション間は `--space-section`（80px+）の余白のみで区切る
- DM Serif Display はセクション見出し以上のみ
- カードボーダーは `rgba(193,160,123,0.2)` の極薄ラテ色
- active ボタンに `scale(0.97)` フィードバック
- 日本語本文の行間は `1.7`〜`1.8`

### Don't

- 純黒 `#000000` をテキストやCTAに使わない — `--espresso` が最暗色
- 純白 `#FFFFFF` をページキャンバスに使わない — カードサーフェスのみ
- グラデーションをヒーロー以外に使わない
- ドロップシャドウに純黒 `rgba(0,0,0,_)` を使わない
- 装飾的ディバイダー線でセクションを区切らない
- h3以下に DM Serif Display を使わない
- 寒色（ブルー・パープル）を導入しない
- 日本語本文の行間を `1.5` 以下にしない

---

## 11. ページ別ガイド

### `/` — Latteシリーズトップ

最もミニマルなページ。Ollamaのホームページに倣い、内容は「ブランド名 + 一文タグライン + アプリカード」のみ。

- ヒーロー: グラデーション背景、DM Serif見出し「Latte」、タグライン1行
- アプリ一覧: 640px幅の単一カラムにカード型リンクを縦積み
- フッター: コピーライトのみの最小構成

### `/voice/` — VoiceLatte LP

プロダクトの全体像を伝えるメインLP。

- ヒーロー: フルビューポート高、アイコン画像 + タイトル + CTAボタン2つ
- Features: 3列グリッド（auto-fit）
- プライバシーバンド: ウォームバンド + バッジ + 1行メッセージ
- Pro: 2列カード
- フッター: リンク群 + コピーライト

### `/voice/privacy/` — プライバシーポリシー

法的ドキュメント。装飾を最小限に。

- ヘッダー: ロゴリンクのみ
- 本文: 720px幅、h2にborder-bottom `2px solid var(--cream)`
- 言語切り替え: 右寄せpillリンク
- コンタクト: `--window-bg` 背景の角丸ボックス（メールアドレスは載せない）

---

## 12. 将来の拡張メモ

- Latteシリーズにアプリが増えたら、トップページにカードを追加（Ollama的にフラットに並べるだけ）
- App Storeリンクボタンの追加時は、Primary pill と同じスタイルで黒系バッジを使用
- brewやcurl等のインストールコマンドがあれば、Ollamaの `install-snippet` pill コンポーネントを導入
- グローバルナビはアプリ3つ以上になった時点で検討
